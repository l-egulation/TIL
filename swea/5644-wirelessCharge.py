'''
[초기 문제 풀이 과정]

이동할 때는 입력받은 방향으로 이동한 후, 그 좌표를 다시 현재 좌표에 할당
> for문 안에서 반복하는 식으로?

dr, dc 설정하고 사용자 이동정보를 idx로 사용해서 매초 위치를 이동시킴
> 이동시키고 BC 범위 내에 있다면 충전량 초마다 작성?

근데 로직만 정확하면 사용자 A,B 각각 충전량 생길 때마다 += 하면됨
> 근데 사용자가 겹치는 상황에서 BC가 2개라면 둘 중에 선택해서 따로 따로 충전량 2개로 분리하고
> BC가 하나라면 해당 BC의 충전량을 반씩 나눠서 더해야함
>> 이 로직이 핵심이고, 잘 작동해야 리스트 없이 이동하자마자 바로 더할 수 있음

의문점 1. BC에서부터 현재 좌표의 거리를 매번 계산해야하는가?
> 계산을 안하면 지금 내가 충전 범위에 있는지 없는지 모름
> 매번 계산을 하자니 BC가 한 두개도 아니고 모든 BC의 개수만큼 매초 계산을 해야함
> 아니면 어짜피 10x10인데 그냥 visited처럼 10x10만들고 BC위치에 충전량만큼 숫자 써두는 것
>> 생각해보니까 이러면 매초마다 if area[r][c] != 0 해서 계속 충전량을 찾고 더해야되는데 매번 10x10순회하는거 아닌지?
>> 그리고 겹치는 부분은 어떻게 할지 고민
>> 한 좌표에 [0, 1] 이렇게 2개로 분리할까 했는데, 어디가 겹칠 줄 알고 이걸 만들지 모르겠음
>> get처럼 없으면 만드는거 없나?
>> BC의 위치와 그 위치에 따른 충전량이 있는 10x10 area를 만들고, 그 위를 순회하면 괜찮지 않을까?

for a, b in 사용자 움직임 리스트:
    curr_ar = curr_ar + dr[a]
    curr_ac = curr_ac + dc[a]
    curr_br = curr_br + dr[b]
    curr_bc = curr_bc + dc[b]

이런 식으로 위치를 바꾸고 해당 위치에서 area[r][c]해서 해당 값을 각 사용자의 전체 충전 값에 += 하는 것
그리고 충전 값이 2개있는 위치에서 해당 시간에 혼자 있다면 둘 중에 큰 것 선택
겹치는 위치에서 사용자가 2명 있다면 하나씩 가져가기

매초 마다 사용자 2명이 각각 위치 이동해서 겹치는 지 확인하는게 저 위에 for문으로 되나?
> if curr_ar == curr_br and curr_ac == curr_bc 하면 될 듯?

------------------------------------------------------------------------------------------------

[코드 설계도 및 회고록]

# 1. 핵심 설계 포인트 (Key Realizations)

(1) 좌표계의 일치:
    - 문제는 (X, Y)로 주어지지만 리스트 인덱스는 (R, C)임.
    - 델타 배열(dr, dc)을 정의할 때 문제의 방향(1:상, 2:우...)과 인덱스 변화량을 정확히 매칭하는 것이 시작임.

(2) 시간의 흐름 (T=0의 중요성):
    - 사용자는 0초(초기 위치)부터 이미 충전 범위에 있을 수 있음.
    - 총 이동 횟수가 M이라면, 충전 기회는 0초부터 M초까지 총 'M+1'번임.

(3) 겹치는 BC 처리 (조합의 원리):
    - 한 사용자가 여러 BC 범위에 동시에 들어갈 수 있음.
    - 두 사용자가 같은 BC를 공유하면 충전량을 반씩 나누지만,
        > 결국 "두 사람 충전량의 합"을 구하는 것이므로 그냥 해당 BC의 파워 P와 같음.
    - 가장 안전한 방법은 A의 선택지(i)와 B의 선택지(j)를 모든 조합으로 맞춰보는 '전수 조사'임.

--------------------------------------------------

# 2. 상세 로직 흐름 (Step-by-Step)

1. [입력]
    - 사용자의 이동 정보와 BC의 정보를 저장함.
    - BC 정보는 (R, C, 범위, 파워) 튜플/리스트 형태로 관리.

2. [M+1번의 반복문]
    - 매 초마다 다음 과정을 수행:

    a. [접속 가능 BC 리스트업]
        - 사용자 A, B의 현재 위치에서 모든 BC와의 거리를 계산함.
        - 거리 공식: D = |r1 - r2| + |c1 - c2| (맨해튼 거리)
        - D <= 범위(C) 인 모든 BC의 '인덱스'를 각자의 후보 리스트에 담음.

    b. [최적 조합 탐색]
        - A의 후보와 B의 후보를 이중 for문으로 순회함.
        - 만약 같은 BC(i == j)를 골랐다면? 합계 = P_i (나눠 가져도 합은 그대로)
        - 만약 서로 다른 BC(i != j)를 골랐다면? 합계 = P_i + P_j
        - 이 중 가장 큰 합계(Max)를 찾아 전체 결과에 누적함.

    c. [위치 이동]
        - M초가 되기 전까지는 입력받은 방향대로 A, B의 좌표를 업데이트함.

--------------------------------------------------

# 3. 셀프 피드백 (Self-Review)

[초기 아이디어 vs 최종 로직 회고]

1. [이동과 충전의 순서]
- 초기 생각: "방향대로 이동한 후, 그 좌표를 현재 좌표에 할당하며 충전량을 더한다."
- 피드백:
    - 사용자는 '0초'인 초기 위치에서도 충전 범위에 있을 수 있음.
    > 따라서 [이동 후 충전]이 아니라 [현재 위치에서 충전 -> 다음 위치로 이동] 순서로
    M+1번 반복하는 구조가 데이터 누락이 없음.

2. [거리 계산 vs 10x10 지도 생성 (의문점 1)]
- 초기 생각: "매번 거리를 계산하기 부담스러우니 10x10 지도를 만들어 충전량을 써두자."
- 피드백:
    - 10x10 지도를 만드는 방식의 한계는 '중첩 범위'임. 한 칸에 2~3개의 BC가 겹칠 수 있는데,
    지도를 쓰려면 한 칸에 [BC1, BC2] 식의 리스트를 담아야 함.
    > 결국 매초 8개의 BC와 거리를 계산(100초 * 8개 = 800번)하는 것이 지도를 탐색하는 것보다
    연산량이 훨씬 적고 구현이 단순함. "계산 안 하면 모른다"는 초기 판단이 옳았음.

3. [사용자 겹침 확인: 위치 vs 리소스]
- 초기 생각: "사용자 A, B의 좌표가 같은지(ar == br) 확인해서 처리하자."
- 피드백:
    - 이 문제의 핵심은 '좌표의 겹침'이 아니라 '사용 가능한 BC의 중복'임.
    - 좌표가 달라도 같은 BC를 쓸 수 있고, 좌표가 같아도 서로 다른 BC를 선택할 수 있음.
    > 따라서 좌표 비교(if ar == br) 대신, 각자가 쓸 수 있는 'BC 인덱스 리스트'를 뽑고
    그 안에서 조합(i == j)을 비교하는 것이 논리적으로 완벽함.

4. [중복 BC의 충전량 분배]
- 초기 생각: "BC가 하나면 반씩 나눠서 각각 += 한다."
- 피드백:
    - 전체 합산량(total_sum)을 구하는 문제이므로, 한 BC를 나눠 가지면
    (P/2) + (P/2) = P가 됨. 즉, i == j인 경우에는 그냥 P값 하나만 더하면 됨.
    - 최종 코드의 이중 for문 조합 탐색은 이 '나눠 갖기'와 '따로 갖기'의 모든 경우의 수를
    전수 조사하여 최댓값을 찾아내는 가장 안전한 설계였음.

5. [데이터 구조의 선택]
- 초기 생각: "딕셔너리나 튜플로 앞의 2개(좌표)는 묶고 뒤는 따로 저장하자."
- 피드백:
    - BC 정보를 [X, Y, 범위, 파워] 리스트로 관리하되, 인덱스(j)를 활용해
    사용자의 접속 가능 목록(charge_idxs)을 만든 것이 좋았음.
    - 덕분에 복잡한 if-else 없이 인덱스 비교만으로 중복 처리가 가능해짐.
'''

T = int(input())

# 이동 방향 정의 (0:제자리, 1:상, 2:우, 3:하, 4:좌)
# 문제의 (x, y) 좌표계와 리스트의 (r, c) 인덱스가 다르므로 이를 고려하여 설정함
dr = [0, 0, 1, 0, -1]
dc = [0, -1, 0, 1, 0]

for tc in range(1, T+1):
    # 이번 테스트 케이스의 총 충전 합산량을 저장할 변수
    answer = 0

    # M: 이동 횟수(초), N: 배터리 충전기(BC)의 개수
    M, N = map(int, input().split())

    # 사용자 A와 B의 매 초 이동 방향 정보를 리스트로 받음
    pos_A = list(map(int, input().split()))
    pos_B = list(map(int, input().split()))

    # BC의 정보를 저장할 리스트
    BC_info = [0]*N
    for i in range(N):
        # [x, y, 범위, 충전량] 순서로 저장됨
        BC_info[i] = list(map(int, input().split()))

    # 사용자의 현재 위치를 저장
    # 위치가 바뀌기 때문에 튜플이 아닌 리스트로 설정
    # A는 (1,1), B는 (10,10)에서 시작
    # 시작 위치가 각각 정해져있기 때문에 초기값 설정
    # [행, 열] 형태의 리스트로 만들어 위치 변화를 실시간으로 반영
    human_rcs = [[1, 1], [10, 10]]

    # 0초(초기 위치)부터 M초까지 총 M+1번 충전을 시도
    # 충전 > M+1 > 초기 위치에서부터 충전이 가능해서 M+1
    for time in range(M+1):
        # 충전을 하고 이동
        # 충전 가능한 위치를 탐색 > 최적 충전량을 충전 > 이동

        # [1단계: 충전 가능한 BC 찾기]

        # charge_idxs[0]은 사용자 A가, [1]은 B가 접속 가능한 BC의 인덱스 번호를 담음
        charge_idxs = [[], []]

        # 두 명의 사용자에 대해 반복
        # A, B 중 누구를 돌지 > 0 / 1
        # i : A인지 B인지 > 사람 번호
        for i in range(2):
            # 현재 사용자의 위치
            r, c = human_rcs[i]

            # 모든 BC를 검사
            # 어떤 충전소인지 선택
            # j : 충전소 번호
            for j in range(N):
                BC_r, BC_c, coverage, charge_amount = BC_info[j]

                # 맨해튼 거리 계산: |X1 - X2| + |Y1 - Y2|
                # 거리가 BC의 충전 범위(coverage) 이내라면 목록에 추가
                if abs(r-BC_r) + abs(c-BC_c) <= coverage:
                    charge_idxs[i].append(j)

        # [2단계: 최적의 충전 조합 결정 (조합 전수 조사)]

        # 이번 초에 A와 B가 얻을 수 있는 최대 충전 합계
        charge = 0

        # a. 사용자 A는 접속 가능한 BC가 없고 B만 있는 경우
        if not charge_idxs[0]:

            # 사용자 B의 접속가능한 BC를 순회
            for i in charge_idxs[1]:
                # 지금 내가 보고 있는 BC의 파워가 다른 파워들보다 더 큰지 판별
                if BC_info[i][3] > charge:
                    charge = BC_info[i][3]

        # b. 사용자 B는 접속 가능한 BC가 없고 A만 있는 경우
        elif not charge_idxs[1]:

            # 사용자 A의 접속가능한 BC를 순회
            for i in charge_idxs[0]:
                if BC_info[i][3] > charge:
                    charge = BC_info[i][3]

        # c. A와 B 둘 다 접속 가능한 BC가 하나 이상 있는 경우
        else:

            # A가 선택 가능한 모든 BC(i)와 B가 선택 가능한 모든 BC(j)를 대조
            # i : A의 충전소 번호
            for i in charge_idxs[0]:
                # j : B의 충전소 번호
                for j in charge_idxs[1]:

                    # [중복 상황] 같은 BC를 선택한 경우
                    if i == j:
                        # 둘이 나눠 가져도 결국 합산은 BC 한 개의 충전량과 같음
                        if BC_info[i][3] > charge:
                            charge = BC_info[i][3]
                    # [개별 상황] 서로 다른 BC를 선택한 경우
                    else:
                        # 각자의 충전량을 온전히 더함
                        if BC_info[i][3] + BC_info[j][3] > charge:
                            charge = BC_info[i][3] + BC_info[j][3]

        # 이번 초에 얻은 최적의 충전량을 총합에 더함
        answer += charge

        # [3단계: 사용자 이동]

        # M초일 때는 마지막 충전 후 루프를 종료 (이동하지 않음)
        if time == M:
            break

        # A와 B의 위치를 다음 방향으로 이동
        human_rcs[0][0] += dr[pos_A[time]]
        human_rcs[0][1] += dc[pos_A[time]]

        human_rcs[1][0] += dr[pos_B[time]]
        human_rcs[1][1] += dc[pos_B[time]]

    print(f'#{tc} {answer}')