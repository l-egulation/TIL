'''
홀수 : +1 / 짝수 : +2

N개 중에 하나 선택해서 홀수(i%2 == 1)날 이면 +1, 짝수(i%2 == 0)날 이면 +2

그냥 매일 물을 주는게 아니라 가끔 하루는 쉬고 주는 게 더 빠를 때도 있음.
ex. 4 2 : 첫째 날 물을 주면 4 3, 둘째 날 물을 주면 4 5, 셋째날 물을 주면 5 5
    > 근데 첫 날에 쉬고, 둘째 날 물을 주면 +2라서 4 2에서 4 4가 됨
    >> 이 규칙을 파악하는게 중요

N개의 수 중에 제일 큰 수와 비교해서 홀수만큼 차이나면 매일 물주기
> 제일 큰 수와 비교해서 짝수만큼 차이나면 짝수 날에만 물 주기

근데 로직을 짜면서 딱 하나만 선택해서 물을 줘야함
> 리스트 순회하면서 하나 선택
> 제일 큰 수 찾아서 비교
> 제일 큰 수와 얼마나 차이나는지 확인하고, 홀수면 바로 물 주고, 짝수면 짝수 날에만 물주기
>> 1. 제일 큰 수와 선택한 수 비교 후 홀짝 판단 / 2. 현재 일 수가 홀짝에 따라 물을 줄 지말지 판단
>> 홀 홀 -> 물줌 / 홀 짝 -> 물줌 / 짝 홀 -> 물 안줌 / 짝 짝 -> 물 줌
>> 짝 홀 일때 물을 안줌 > 만약 수가 2개가 아니라면 다른 수를 선택해서 물을 줄지 말지 선택할 수 있음
>> 짝 홀 일때 물을 안주니까 다른 수 선택해서 비교 후 홀 수 이면 물주기

while True:
    day += 1

    # trees의 모든 요소가 같으면 종료
    # 그리고 day 출력
    if len(set(trees)) == 1:
        print(f'#{tc} {day}')

    trees.sort()
    high = trees[-1]

    tree = trees[0]

    if tree == high:
        continue
    else:
        # 수 차이 홀수
        if (high-tree)%2 == 1:
            # 날짜 홀수
            if day%2 == 1:
                tree += 1
            # 날짜 짝수
            elif day%2 == 0:
                tree += 2
        # 수 차이 짝수
        elif (high-tree)%2 == 0:
            # 날짜 홀수
            if day%2 == 1:
                continue
            # 날짜 짝수
            elif day%2 == 0:
                tree += 2

--------------------------------------------------

# 1. 핵심 설계 포인트 (Key Realizations)

(1) 시뮬레이션 vs 수학:
    - 매일 어떤 나무를 줄지 정하는 시뮬레이션은 나무의 높이 차이가 클 때 시간 초과 위험이 큼
    - 필요한 +1의 총량(ones)과 +2의 총량(twos)을 미리 계산하는 것이 핵심

(2) 티켓의 유연성 (Balancing):
    - +2cm 성장은 상황에 따라 +1cm 두 번으로 쪼갤 수 있음. (단, 그 반대는 안 됨)
    - 짝수 날(+2)만 너무 많으면 홀수 날이 비게 되므로, 2 하나를 버리고 1 두 개를 가져와서
    > 비어있는 홀수 날을 채우는 것이 최적화의 핵심

(3) 목표 높이의 함정:
    - 반드시 현재 max_height가 정답이 아닐 수 있음
    - 홀수 날 티켓이 너무 많이 남을 경우, 목표를 1cm 높여서 남는 1들을 2로 합쳐 쓰는 게
    > 더 빠를 수 있음. 따라서 target과 target + 1을 모두 검사해야 함

--------------------------------------------------

# 2. 날짜 계산 공식 유도

- ones == twos: [1, 2, 1, 2] -> 2 * ones
- ones > twos:  [1, 2, 1, 2, 1] -> 2 * ones - 1 (마지막이 홀수 날)
- twos > ones:  [1, 2, 1, 2, 0, 2] -> 2 * twos (짝수 날 티켓이 남으므로 그만큼 일수 증가)

--------------------------------------------------

# 3. 셀프 피드백 (Self-Review)

- 원래 생각했던 로직의 문제점:
    "매일 나무 하나를 선택해 홀짝을 맞춘다"는 시뮬레이션 방식은 직관적이지만,
    특정 날에 물을 안 주는 것이 나중에 더 큰 이득이 되는 '미래의 상황'을 모두 고려하기 어려움.
    이로 인해 불필요한 sort와 조건문이 많아져 시간 초과가 발생

- 수정 및 보완된 점:
    1) '어떤 나무에 줄까?'라는 고민을 '총 몇 번의 +1과 +2가 필요한가?'로 치환

    2) 짝수 날에만 물을 줘야 하는 상황(수 차이 짝수)에서 억지로 기다리는 로직 대신,
        남는 짝수 티켓을 홀수 날 두 번으로 쪼개는 '밸런싱' 개념을 도입
        이것이 원래 생각했던 "하루 쉬고 주는 게 더 빠를 때도 있음"이라는 규칙의 수학적 해답

    3) "제일 큰 수와 비교해서 홀짝 판단"하는 방식은 유효했으나, 목표 높이 자체가
        유동적일 수 있다는 점을 간과했음. target + 1 케이스를 추가하여 이를 보완

- 결론:
    시뮬레이션으로 풀리지 않는 문제는 자원의 총량을 구한 뒤,
    그 자원들 간의 변환 가능성(2 -> 1, 1)을 이용해 최적의 균형점을 찾는 것이 정답에 가까움
'''

T = int(input())

for tc in range(1, T + 1):
    N = int(input())
    trees = list(map(int, input().split()))

    # 현재 나무 중 가장 높은 키
    max_h = max(trees)
    # 최솟값을 저장할 변수
    min_total_days = float('inf')

    # 목표 높이가 max_h일 때와 max_h + 1일 때 두 경우를 모두 확인
    for target in [max_h, max_h + 1]:
        ones = 0 # +1이 필요한 총 횟수
        twos = 0 # +2가 필요한 총 횟수

        for tree in trees:
            diff = target - tree
            ones += diff % 2     # 홀수 차이는 +1 티켓 1개 필수
            twos += diff // 2    # 짝수 차이는 일단 +2 티켓으로 계산

        # +2 티켓을 +1 티켓 2개로 쪼개서 밸런스를 맞춤
        # 2cm 성장은 1cm 성장 두 번으로 대체 가능하므로,
        # 2의 개수가 1보다 너무 많으면 쪼개서 빈 날짜(홀수 날)를 채움
        if ones < twos:
            while abs(ones - twos) > 1:
                twos -= 1
                ones += 2
                if ones > twos: # 1이 더 많아지는 순간 멈춤
                    break

        # 최종 날짜 계산 공식
        if ones > twos:
            # 1이 더 많으면: 1, 2, 1, 2, 1... 마지막이 홀수 날
            current_days = ones * 2 - 1
        elif twos > ones:
            # 2가 더 많으면: 1, 2, 1, 2, (공백), 2... 마지막이 짝수 날
            current_days = twos * 2
        else:
            # 1과 2의 개수가 같으면: 1, 2, 1, 2... 딱 맞음
            current_days = ones * 2

        # 두 목표 높이 중 더 적게 걸리는 날짜를 선택
        if current_days < min_total_days:
            min_total_days = current_days

    print(f"#{tc} {min_total_days}")